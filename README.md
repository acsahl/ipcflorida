# IPC Florida — Daily Bible Reading (2026)

Vite + React frontend, Netlify Functions backend, MongoDB Atlas storage.

## Local dev

```bash
npm install
cp .env.example .env   # fill in MONGODB_URI, MONGODB_DB, JWT_SECRET
netlify dev
```
